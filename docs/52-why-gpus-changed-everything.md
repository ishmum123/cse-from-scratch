# Why GPUs Changed Everything

## The Problem

CPUs cannot train billion-parameter models in a human lifetime.

## Challenge

Implement the smallest program that demonstrates 'GPUs Changed Everything' before reading the solution.

## Exercise

Write a minimal implementation of 'GPUs Changed Everything', then measure where it breaks.

---

## Engineering Notes

This discovery exists because the previous approach failed under a real constraint. The lesson is not 'GPUs Changed Everything' as a fact, but as a response to pressure.

---

**Continue → Why Matrix Multiplication Matters**
